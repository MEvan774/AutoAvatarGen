# Media Library — files available for media tags

> Auto-generated by `generate_media_library.py` on 2026-07-25 10:21.
> Media root: `C:\Users\Milan\Documents\GitHub\AutoAvatarGen\Assets\Python\script`

**Give this file to the model together with `SCRIPT_TAG_GUIDE.md`** before asking it to write `Script.txt`.

Rules for the model:

- Use **only** the names listed below in media tags. Every name is a real file on disk — **do not invent names** and do not reuse placeholder names like `ArticleTemp` / `VideoTemp`.
- Type the name **exactly as shown, without the file extension**, e.g. `{Image:<name>,4}`. Match the casing.
- If a needed image or clip isn't listed here, it doesn't exist yet — pick the closest listed name or leave the tag out.

---

## At a glance

| Folder | Files | Tags |
|---|---|---|
| `Images/` | _not found_ | `{Image:}` `{BigImage:}` |
| `Logos/` | _not found_ | `{Image:}` `{BigImage:}` |
| `BRoll/` | _not found_ | `{Video:}` |

---

---

_Total resolvable media files: 0._
